#!/bin/bash -l
set -euo pipefail

jupyter server extension enable --sys-prefix nbgitpuller
playwright install chromium

mkdir -p usr/local/share/jupyter/lab/settings
cp -p overrides.json usr/local/share/jupyter/lab/settings
