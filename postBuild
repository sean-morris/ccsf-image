#!/bin/bash -l
set -euo pipefail

jupyter server extension enable --sys-prefix nbgitpuller
playwright install chromium

mkdir -p opt/conda/share/jupyter/lab/settings
cp -p overrides.json opt/conda/share/jupyter/lab/settings
